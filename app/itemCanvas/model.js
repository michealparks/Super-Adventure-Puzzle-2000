import {canvas} from 'canvas/controller';

export const itemCanvas = document.document.getElementById('item-canvas');
export const itemCtx    = cacheCanvas.getContext('2d');

cacheCanvas.height = canvas.height;
cacheCanvas.width  = canvas.width;
